// backend/event/BackendEvent.java
package backend.event;

/**
 * Clase base abstracta para todos los eventos que el backend puede notificar al frontend.
 * Esto permite una comunicación estructurada y extensible.
 */
public abstract class BackendEvent {
    // Podría contener un timestamp, ID de evento, etc. si fuera necesario para depuración o auditoría.
}